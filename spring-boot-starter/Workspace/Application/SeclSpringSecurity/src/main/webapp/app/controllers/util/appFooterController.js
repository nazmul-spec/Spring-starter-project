
'use strict';

define(['app'], function (app) {
	
	var appFooterController = function ($rootScope, $scope, localize, languageService, constantService,
			configurationService, localStorageService, authorizationService) {
		
		
		
		var init = function(){
			
			
		};
		
		init();
			 
    };

    app.controller('appFooterController', ['$rootScope', '$scope', 'localize', 'languageService', 'constantService', 
    'configurationService', 'localStorageService', 'authorizationService', appFooterController]);
   
	
});














